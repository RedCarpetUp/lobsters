<%= @user == @applquestion.asker ? "Your" : @applquestion.asker.username+"\'s" %> question to <%= @application.name %> in hiring for job <%= @application.job.title %> has been answered.

To see this answer, go to link below:

<%= Rails.application.root_url %>jobs/<%= @application.job.id %>/applications/<%= @application.id %>/appl_questions/<%= @applquestion.urlkey %>